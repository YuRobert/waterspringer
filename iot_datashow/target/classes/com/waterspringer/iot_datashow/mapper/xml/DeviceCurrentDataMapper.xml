<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.waterspringer.iot_datashow.mapper.DeviceCurrentDataMapper">

    <select id="getAllCurrentDataByCompany"
            resultType="com.waterspringer.iot_datashow.entity.DeviceCurrentData">
        SELECT
            dcd.*
        FROM
            device_details dd
                LEFT JOIN `device_current_data` dcd ON dd.`device_id` = dcd.`device_id`
        WHERE
            produce_company = #{company_name} and !ISNULL (dcd.`device_id`)
    </select>
</mapper>
